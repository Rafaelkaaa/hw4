SELECT NAME,
BIRTHDAY,
'YOUNGEST' AS TYPE
FROM WORKER
WHERE BIRTHDAY IN(
SELECT MAX
(BIRTHDAY)
FROM WORKER 
)
UNION ALL
SELECT NAME,
BIRTHDAY,
'ELDEST' AS TYPE
FROM WORKER
WHERE BIRTHDAY IN(
SELECT MIN
(BIRTHDAY)
FROM WORKER 
);